app
		.controller(
				'homeCtrl',
				function($scope, $rootScope, $routeParams, $location, $http,
						Data, $interval, $localStorage) {
					// alert("homeCtrl");
					$scope.init = function() {

					};


					$scope.init();
				});